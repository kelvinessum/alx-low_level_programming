#include "main.h"
/**
 * _isupper - Checks for uppercase characeter
 *
 * Return : Always success
 */

int _isupper(int c)
{

	if (c = 'A'; c <= 'Z')
	{
		_putchar(1);
	}

	else
	{
		_putchar(0);
	}
	return (0);
}
