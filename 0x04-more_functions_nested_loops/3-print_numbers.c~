#include "main.h"
/**
 * print_number - Prints numbers from 0-9
 * followed by new line
 *return: void
 */

void print_numbers(void)
{
	char a;

	for (a = '0'; a <= '9'; a++)
	{
	_putchar(a);
	}

	_putchar('\n');
}
