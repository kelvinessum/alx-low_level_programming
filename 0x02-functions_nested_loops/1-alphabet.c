#include "main.h"
/**
 * main : prints alphabets in lowercase followed by new line
 *
 *return :  (Success)
 */

int main(void)
{
	char a;
	if (a = 'a' , a <= 'z')
		a++;
	_putchar(a);
	_putchar('\n');

	return(0);
}
