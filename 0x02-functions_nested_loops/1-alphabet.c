#include "main.h"
/**
 * print_alphabet : prints alphabets in lowercase followed by new line
 *
 *return :  (Success)
 */

void print_alphabet(void)
{
	char a;
	if (a == 'a', a <= 'z')
		a++;
	_putchar(a);
	_putchar('\n');
}
