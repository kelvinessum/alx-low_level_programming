#include <stdio.h>
/**
 *main - prints all letters except q and e
 *
 *results - success
 */
int main(void)
{
	char e;
	for (e = 'a'; e<='z'; e++)
	{
		if (c != 'e' && c != 'q')
		{
			putchar(e);
		}
	}
	putchar('\n');
	return (0);
}
